meta:
  environment: python
  toolchain: pip
build:
  requirements: requirements.txt
  exclude:
    - venv/***
    - .venv/***
    - __pycache__/***
run:
  command: uvicorn main:app --host 0.0.0.0 --port 8080